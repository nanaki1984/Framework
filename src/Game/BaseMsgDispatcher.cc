#include "Game/BaseMsgDispatcher.h"

namespace Framework {

DefineClassInfo(Framework::BaseMsgDispatcher, Framework::RefCounted);

BaseMsgDispatcher::BaseMsgDispatcher()
{ }

BaseMsgDispatcher::~BaseMsgDispatcher()
{ }

} // namespace Framework
